# ui-auth

This directory is an inactive feature placeholder and is intentionally not included in `settings.gradle.kts`.

Before adding source here, create a module `build.gradle.kts`, include `:feature:ui-auth` in `settings.gradle.kts`, and add module-level verification.
