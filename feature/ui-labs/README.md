# ui-labs

This directory is an inactive feature placeholder and is intentionally not included in `settings.gradle.kts`.

The existing graph asset is retained as historical context only. Before adding source here, create a module `build.gradle.kts`, include `:feature:ui-labs` in `settings.gradle.kts`, and add module-level verification.
