dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = 'nandaDiagnosis'
include(
        ":app",
        ":data:db",
        ":feature:error",
        ":feature:kakao_search",
        ":feature:reference_search",
        ":common",
)


include ':feature:reference_search'
